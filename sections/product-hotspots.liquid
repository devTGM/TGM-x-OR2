{%- liquid
  assign section_bg   = section.settings.background_color | default: '#F5F0EC'
  assign accent_color = section.settings.accent_color | default: '#1C1C1C'
-%}

<section
  class="phs-section section-wrapper"
  id="ProductHotspots-{{ section.id }}"
  style="background-color: {{ section_bg }};"
>
  {%- if section.settings.overline != blank or section.settings.heading != blank -%}
    <div class="phs__intro">
      {%- if section.settings.overline != blank -%}
        <p class="phs__overline">{{ section.settings.overline }}</p>
      {%- endif -%}
      {%- if section.settings.heading != blank -%}
        <h2 class="phs__heading">{{ section.settings.heading }}</h2>
      {%- endif -%}
    </div>
  {%- endif -%}

  <div class="phs__layout">

    <!-- LEFT: Image + pins -->
    <div class="phs__image-col">
      <div class="phs__image-wrap" id="PhsImageWrap-{{ section.id }}">

        {%- if section.settings.product_image != blank -%}
          {{
            section.settings.product_image
            | image_url: width: 1200
            | image_tag:
              class: 'phs__product-img',
              loading: 'lazy',
              widths: '400,600,800,1000,1200',
              sizes: '(max-width: 749px) 100vw, 55vw'
          }}
        {%- else -%}
          {{ 'product-1' | placeholder_svg_tag: 'phs__product-img phs__product-img--placeholder' }}
        {%- endif -%}

        {%- for block in section.blocks -%}
          <button
            class="phs__pin{% if forloop.first %} is-active{% endif %}"
            data-phs-pin="{{ forloop.index }}"
            data-section="{{ section.id }}"
            style="left:{{ block.settings.pin_x | default: 50 }}%;top:{{ block.settings.pin_y | default: 30 }}%;--pin-accent:{{ accent_color }};"
            aria-label="{{ block.settings.label | default: 'Feature' }}"
            aria-pressed="{% if forloop.first %}true{% else %}false{% endif %}"
            {{ block.shopify_attributes }}
          >
            <span class="phs__pin-ring"></span>
            <span class="phs__pin-number">{{ forloop.index }}</span>
          </button>
        {%- endfor -%}

      </div>
    </div>

    <!-- RIGHT: Detail panel -->
    <div class="phs__detail-col" id="PhsDetailCol-{{ section.id }}">
      {%- for block in section.blocks -%}
        <div
          class="phs__detail{% if forloop.first %} is-active{% endif %}"
          data-phs-detail="{{ forloop.index }}"
          data-section="{{ section.id }}"
          aria-hidden="{% if forloop.first %}false{% else %}true{% endif %}"
          {{ block.shopify_attributes }}
        >
          <div class="phs__detail-inner">

            <div class="phs__detail-header">
              <span class="phs__detail-num" style="--pin-accent:{{ accent_color }};">{{ forloop.index }}</span>
              <span class="phs__detail-label">{{ block.settings.label | default: 'Feature' }}</span>
            </div>

            {%- if block.settings.detail_heading != blank -%}
              <h3 class="phs__detail-heading">{{ block.settings.detail_heading }}</h3>
            {%- endif -%}

            {%- if block.settings.detail_body != blank -%}
              <div class="phs__detail-body rte">{{ block.settings.detail_body }}</div>
            {%- endif -%}

            {%- if block.settings.detail_image != blank -%}
              <div class="phs__detail-img-wrap">
                {{
                  block.settings.detail_image
                  | image_url: width: 600
                  | image_tag:
                    class: 'phs__detail-img',
                    loading: 'lazy',
                    widths: '280,400,600',
                    sizes: '(max-width: 749px) 80vw, 280px'
                }}
              </div>
            {%- endif -%}

            {%- if block.settings.cta_text != blank and block.settings.cta_url != blank -%}
              <a href="{{ block.settings.cta_url }}" class="phs__detail-cta">
                {{ block.settings.cta_text }}<span class="phs__cta-arrow" aria-hidden="true">&#8594;</span>
              </a>
            {%- endif -%}

          </div>
        </div>
      {%- endfor -%}
    </div>

  </div>
</section>

<style>
  /* ─── Layout shell ─────────────────────────────────────── */
  .phs-section {
    padding: {{ section.settings.padding_top }}px var(--page-margin, 24px) {{ section.settings.padding_bottom }}px;
  }

  .phs__intro {
    text-align: center;
    margin: 0 auto clamp(36px, 5vw, 60px);
    max-width: 640px;
  }

  .phs__overline {
    font-family: var(--font-body-custom--family, 'Red Hat Text', sans-serif);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #8A8580;
    margin: 0 0 10px;
  }

  .phs__heading {
    font-family: var(--font-heading--family, Georgia, serif);
    font-size: clamp(1.6rem, 3.5vw, 2.6rem);
    font-weight: 400;
    line-height: 1.2;
    letter-spacing: -0.01em;
    color: #1C1C1C;
    margin: 0;
  }

  .phs__layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: 40px;
    max-width: 1200px;
    margin: 0 auto;
    align-items: center;
  }

  @media screen and (min-width: 750px) {
    .phs__layout {
      grid-template-columns: {{ section.settings.image_width }}fr {{ 10 | minus: section.settings.image_width }}fr;
      gap: clamp(40px, 6vw, 88px);
    }
  }

  /* ─── Image wrap ────────────────────────────────────────── */
  .phs__image-wrap {
    position: relative;
    width: 100%;
    aspect-ratio: {{ section.settings.image_aspect_ratio }};
    border-radius: {{ section.settings.image_radius }}px;
    overflow: hidden;
    background: #EAE6DF;
  }

  .phs__product-img,
  .phs__product-img--placeholder {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .phs__product-img--placeholder { opacity: 0.4; }

  /* ─── Pins ──────────────────────────────────────────────── */
  .phs__pin {
    position: absolute;
    transform: translate(-50%, -50%);
    width: 36px;
    height: 36px;
    border-radius: 50%;
    border: none;
    background: transparent;
    cursor: pointer;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10;
    transition: transform 0.25s ease;
  }
  .phs__pin:hover { transform: translate(-50%, -50%) scale(1.12); }

  .phs__pin-ring {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    border: 2px solid rgba(255,255,255,0.75);
    background: rgba(255,255,255,0.22);
    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);
    box-shadow: 0 2px 14px rgba(0,0,0,0.22);
    transition: background 0.25s ease, border-color 0.25s ease;
  }

  .phs__pin.is-active .phs__pin-ring,
  .phs__pin:hover .phs__pin-ring {
    background: var(--pin-accent, #1C1C1C);
    border-color: var(--pin-accent, #1C1C1C);
  }

  .phs__pin-number {
    position: relative;
    z-index: 1;
    font-family: var(--font-body-custom--family, 'Red Hat Text', sans-serif);
    font-size: 13px;
    font-weight: 600;
    color: #1C1C1C;
    transition: color 0.25s ease;
    line-height: 1;
  }
  .phs__pin.is-active .phs__pin-number,
  .phs__pin:hover .phs__pin-number { color: #fff; }

  /* Pulse animation on inactive pins */
  .phs__pin:not(.is-active) .phs__pin-ring::after {
    content: '';
    position: absolute;
    inset: -5px;
    border-radius: 50%;
    border: 1.5px solid rgba(255,255,255,0.55);
    animation: phs-pulse 2.4s ease-out infinite;
    pointer-events: none;
  }
  @keyframes phs-pulse {
    0%   { opacity: 0.85; transform: scale(1); }
    70%  { opacity: 0;    transform: scale(1.55); }
    100% { opacity: 0;    transform: scale(1.55); }
  }

  @media screen and (max-width: 749px) {
    .phs__pin { width: 30px; height: 30px; }
    .phs__pin-number { font-size: 11px; }
  }

  /* ─── Detail column ─────────────────────────────────────── */
  .phs__detail-col {
    position: relative;
    min-height: 260px;
  }

  /* Desktop: absolute-stacked, fade/slide transition */
  @media screen and (min-width: 750px) {
    .phs__detail {
      position: absolute;
      inset: 0;
      opacity: 0;
      pointer-events: none;
      transform: translateY(10px);
      transition: opacity 0.3s ease, transform 0.3s ease;
    }
    .phs__detail.is-active {
      position: relative;
      opacity: 1;
      pointer-events: auto;
      transform: translateY(0);
    }
  }

  /* Mobile: simple show/hide */
  @media screen and (max-width: 749px) {
    .phs__detail-col { min-height: unset; }
    .phs__detail { display: none; }
    .phs__detail.is-active { display: block; }
  }

  .phs__detail-inner {
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  /* Number + label row */
  .phs__detail-header {
    display: flex;
    align-items: center;
    gap: 14px;
  }

  .phs__detail-num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    width: 38px;
    height: 38px;
    border-radius: 50%;
    background: var(--pin-accent, #1C1C1C);
    color: #fff;
    font-family: var(--font-body-custom--family, 'Red Hat Text', sans-serif);
    font-size: 14px;
    font-weight: 600;
    line-height: 1;
  }

  .phs__detail-label {
    font-family: var(--font-body-custom--family, 'Red Hat Text', sans-serif);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: #8A8580;
  }

  /* Heading */
  .phs__detail-heading {
    font-family: var(--font-heading--family, Georgia, serif);
    font-size: clamp(1.3rem, 2.4vw, 1.85rem);
    font-weight: 400;
    line-height: 1.25;
    letter-spacing: -0.01em;
    color: #1C1C1C;
    margin: 0;
  }

  /* Body text */
  .phs__detail-body {
    font-family: var(--font-body-custom--family, 'Red Hat Text', sans-serif);
    font-size: 15.5px;
    line-height: 1.75;
    color: #4A4540;
  }
  .phs__detail-body p { margin: 0 0 10px; }
  .phs__detail-body p:last-child { margin-bottom: 0; }

  /* Close-up image */
  .phs__detail-img-wrap {
    overflow: hidden;
    border-radius: 4px;
    max-width: 320px;
  }
  .phs__detail-img {
    width: 100%;
    display: block;
    object-fit: cover;
    aspect-ratio: 4/3;
  }

  /* CTA */
  .phs__detail-cta {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-family: var(--font-body-custom--family, 'Red Hat Text', sans-serif);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: #1C1C1C;
    text-decoration: none;
    border-bottom: 1px solid #1C1C1C;
    padding-bottom: 2px;
    width: fit-content;
    transition: opacity 0.2s ease;
  }
  .phs__detail-cta:hover { opacity: 0.55; }
  .phs__cta-arrow { transition: transform 0.2s ease; display: inline-block; }
  .phs__detail-cta:hover .phs__cta-arrow { transform: translateX(4px); }
</style>

<script>
  (function () {
    var sid     = '{{ section.id }}';
    var imgWrap = document.getElementById('PhsImageWrap-' + sid);
    var detCol  = document.getElementById('PhsDetailCol-' + sid);
    if (!imgWrap || !detCol) return;

    var pins    = Array.from(imgWrap.querySelectorAll('[data-phs-pin][data-section="' + sid + '"]'));
    var details = Array.from(detCol.querySelectorAll('[data-phs-detail][data-section="' + sid + '"]'));

    function activate(idx) {
      pins.forEach(function (p, i) {
        var on = (i === idx);
        p.classList.toggle('is-active', on);
        p.setAttribute('aria-pressed', on ? 'true' : 'false');
      });
      details.forEach(function (d, i) {
        var on = (i === idx);
        d.classList.toggle('is-active', on);
        d.setAttribute('aria-hidden', on ? 'false' : 'true');
      });
    }

    pins.forEach(function (pin, i) {
      pin.addEventListener('click', function () { activate(i); });
    });
  })();
</script>

{% schema %}
{
  "name": "Product Hotspots",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "Section header"
    },
    {
      "type": "text",
      "id": "overline",
      "label": "Overline (small caps)",
      "default": "Craftsmanship"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Made with intention"
    },
    {
      "type": "header",
      "content": "Product image"
    },
    {
      "type": "image_picker",
      "id": "product_image",
      "label": "Product image"
    },
    {
      "type": "select",
      "id": "image_aspect_ratio",
      "label": "Image aspect ratio",
      "options": [
        { "value": "3/4",  "label": "Portrait 3:4" },
        { "value": "4/5",  "label": "Portrait 4:5" },
        { "value": "1/1",  "label": "Square 1:1" },
        { "value": "4/3",  "label": "Landscape 4:3" },
        { "value": "16/9", "label": "Wide 16:9" }
      ],
      "default": "3/4"
    },
    {
      "type": "range",
      "id": "image_width",
      "label": "Image column width (out of 10)",
      "min": 4,
      "max": 7,
      "step": 1,
      "default": 6
    },
    {
      "type": "range",
      "id": "image_radius",
      "label": "Image corner radius",
      "min": 0,
      "max": 24,
      "step": 2,
      "unit": "px",
      "default": 0
    },
    {
      "type": "header",
      "content": "Colours"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Section background",
      "default": "#F5F0EC"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Pin and accent colour",
      "default": "#1C1C1C"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 80
    }
  ],
  "blocks": [
    {
      "type": "hotspot",
      "name": "Hotspot",
      "settings": [
        {
          "type": "header",
          "content": "Pin position"
        },
        {
          "type": "paragraph",
          "content": "Set X (left to right) and Y (top to bottom) as a percentage within the image."
        },
        {
          "type": "range",
          "id": "pin_x",
          "label": "Horizontal position (X %)",
          "min": 2,
          "max": 98,
          "step": 1,
          "unit": "%",
          "default": 50
        },
        {
          "type": "range",
          "id": "pin_y",
          "label": "Vertical position (Y %)",
          "min": 2,
          "max": 98,
          "step": 1,
          "unit": "%",
          "default": 30
        },
        {
          "type": "header",
          "content": "Detail content"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Feature label",
          "default": "Collar",
          "info": "Short uppercase label shown beside the pin number"
        },
        {
          "type": "text",
          "id": "detail_heading",
          "label": "Heading",
          "default": "Italian poplin collar"
        },
        {
          "type": "richtext",
          "id": "detail_body",
          "label": "Body text",
          "default": "<p>Crafted from 100-thread-count European flax, the collar holds its shape through dozens of washes — no starch required.</p>"
        },
        {
          "type": "image_picker",
          "id": "detail_image",
          "label": "Close-up image (optional)"
        },
        {
          "type": "header",
          "content": "Optional link"
        },
        {
          "type": "text",
          "id": "cta_text",
          "label": "Link text",
          "placeholder": "Learn more"
        },
        {
          "type": "url",
          "id": "cta_url",
          "label": "Link URL"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Product Hotspots",
      "blocks": [
        {
          "type": "hotspot",
          "settings": {
            "pin_x": 50,
            "pin_y": 16,
            "label": "Collar",
            "detail_heading": "Italian poplin collar",
            "detail_body": "<p>The collar is fused with a hair-canvas interlining — it holds its shape wash after wash without a drop of starch.</p>"
          }
        },
        {
          "type": "hotspot",
          "settings": {
            "pin_x": 38,
            "pin_y": 44,
            "label": "Fabric",
            "detail_heading": "100% European Flax Linen",
            "detail_body": "<p>Stone-washed at 60 degrees for pre-shrinking and that naturally relaxed drape. Breathable in summer, insulating in winter.</p>"
          }
        },
        {
          "type": "hotspot",
          "settings": {
            "pin_x": 54,
            "pin_y": 68,
            "label": "Buttons",
            "detail_heading": "Corozo nut buttons",
            "detail_body": "<p>Each button is carved from the tagua nut — a sustainable alternative to plastic. Naturally matte, no two are identical.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
